server:
  port: 8080
  tomcat:
    max-threads: 50        # 인스턴스별로 동일하게 고정
    accept-count: 20       # 초과 요청 대기열
    connection-timeout: 2000ms

spring:
  threads:
    virtual:
      enabled: false       # 반드시 false (비교 실험 목적)

logging:
  level:
    root: INFO
